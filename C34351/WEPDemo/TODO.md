# TODO

Make the scripts more robust by checking the status of the wlan0 interface rather than just waiting a delay.

The client script should not start the iperf server unless it can ping the server
